import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.aifoodrecommendation.app',
  appName: 'AI Food Recommendation',
  webDir: 'dist'
};

export default config;
